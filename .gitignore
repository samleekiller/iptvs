node_modules
# package-lock.json  # 不应该忽略 lock 文件，它能锁定依赖版本加速构建

# 备份文件
*.bak

# 个人配置文件
external-sources.json
system-config.json
epg-sources.json
users.json

# 生成的播放列表文件（根据配置自动生成）
interface.txt
interfaceTXT.txt
playback.xml
my-playlist-config.json
my-playlist-config.*.json
my-playlist-profiles.json
built-in-sources-cache.json

# 抓取模块的配置与缓存（不设 mdataDir 时默认写在当前目录）
extractors.json
extractor-cache.json
extractors.json.corrupt
built-in-sources-remote.json
pe-cache.json
# 央视频持久浏览器登录态（等同账号凭据，不进入仓库/JSON 配置备份）
/yangshipin/

# 外部 EPG 源下载缓存（issue #38）
epg-cache/

# 公共台标库索引缓存（issue #124）
logo-index.json

# 持久化数据目录（mdataDir 指向时）
data/

# probe-m3u.mjs 探活脚本生成的报告
probe-report.md
